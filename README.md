# Account-Managment-System-MernStack



please before run the program extract nodemodule zip file inside the client file


run for server(backend) => npm run dev <br><br>
run for client(frontend) => npm start
